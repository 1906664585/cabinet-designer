{
  "name": "@cabinet-designer/core",
  "version": "0.0.1",
  "description": "Core algorithms for cabinet designer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.1.6",
    "vitest": "^0.34.0"
  }
}
